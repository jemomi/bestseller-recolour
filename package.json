{
  "name": "bestseller-recolour",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -k \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
