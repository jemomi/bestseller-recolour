{
  "private": true,
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch ./bin/www",
    "start": "node --env-file-if-exists=.env ./bin/www"
  },
  "dependencies": {
    "express": "~4.16.1"
  }
}
